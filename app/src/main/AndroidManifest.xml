<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.decompany.theworker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity_Employee"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".MainActivity_Manager"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".MainActivity_HR"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".SignupActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".NotificationActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".StatisticsActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".VacationActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".SalaryRaiseActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".SickNoteActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity android:name=".PublishMessage"></activity>
    </application>

</manifest>